# Docs

JSDoc on anything exported. @param and @returns required, @throws when it can throw.
Skip private helpers. Skip getters.

Inline comments explain why, not what. If the comment restates the line, delete the comment.
